# Copy to .env and fill in whichever keys you use.
# eval_llm_wc.py auto-discovers this file from the working directory
# or any parent of the script (override with --env-file /path/to/.env).

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=
